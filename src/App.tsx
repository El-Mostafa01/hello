import "./App.css";
import IOSHome from "./ios/IOSHome";

/**
 * SCINC iOS-Glass — an iOS 18–style "liquid glass" home screen for Android.
 * Install it on a Pixel via the browser's "Add to Home screen" to run it
 * full-screen like a native app.
 */
export default function App() {
  return <IOSHome />;
}
